apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':backend')
    compile project(':bot')
    compile group: 'com.miglayout', name: 'miglayout-swing', version: '5.0'
}
